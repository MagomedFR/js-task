const arr = ['Даниил', 'Ева', 'Тимофей' , 'Варвара', 'Анна', 'Анастасия', 'Варвара'];
for(let i = 0; i < arr.length; i++) {
    if(arr[i][0].toLowerCase() == 'а') {
        console.log(arr[i])
    }
}